[project]
name = "pi-skill-refine"
version = "0.1.0"
description = "Queue continual harness refinement in Pi."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
refine = "refine:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/refine"]
