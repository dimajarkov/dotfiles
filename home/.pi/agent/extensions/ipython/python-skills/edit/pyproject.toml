[project]
name = "pi-ipython-skill-edit"
version = "0.1.0"
description = "Exact single-occurrence edits with structured notebook diffs."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/edit"]
