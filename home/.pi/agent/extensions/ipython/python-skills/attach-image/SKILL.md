---
name: attach-image
description: Load PNG, JPEG, GIF, or WebP files into model context so the model can see them. Use for screenshots and visual inspection.
---

# Attach image

Call `await attach_image(path)` from IPython.
