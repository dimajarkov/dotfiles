[project]
name = "pi-ipython-skill-attach-image"
version = "0.1.0"
description = "Load supported image files into model context."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/attach_image"]
